export type User = {
  username: string,
  taxnumber: string,
  useremail: string,
  userphone: string,
  userpassword: string,
}

export type Product = {
  name: string,
  description: string,
  price: number,
  stock: number,
}